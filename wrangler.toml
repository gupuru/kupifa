name = "kupifa"
compatibility_date = "2026-08-31"
pages_build_output_dir = "docs"

# DMG は Pages の 25MiB 制限を超えることがあるので R2 に置く。
# /download の Pages Function がこのバケットから配信する。
[[r2_buckets]]
binding = "DOWNLOADS"
bucket_name = "kupifa-downloads"
